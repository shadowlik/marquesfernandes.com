---
import BaseLayout from '@/layouts/BaseLayout.astro';
import { useTranslations } from '@/i18n/utils';

const t = useTranslations('pt');
---

<BaseLayout lang="pt" title="MF." description={t('site.tagline')}>
  <h1>MF.</h1>
  <p>{t('site.tagline')}</p>
  <p>Em migração do WordPress para Astro.</p>
</BaseLayout>
